#include "main.h"
#include <stdio.h>

/**
 * set_string - sets value of pointer to char
 * @s: pointer to pointer
 * @to: pointer to char
 * Return: void
 */

void set_string(char **s, char *to)
{
	*s = to
}
